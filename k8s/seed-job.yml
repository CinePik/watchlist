apiVersion: batch/v1
kind: Job
metadata:
  name: cinepik-watchlist-seeding-job
spec:
  template:
    spec:
      containers:
        - name: cinepik-watchlist-seeder
          image: lzukanovic/cinepik-watchlist:latest
          command: ['npm', 'run', 'prisma:seed']
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-credentials
                  key: DATABASE_URL
      restartPolicy: Never
  backoffLimit: 2
